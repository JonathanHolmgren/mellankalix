
<section class="movie-details-2">
  <div>
  <h1 class="movie-title-2">{{ movie.attributes.title }}</h1>
  <p class="movie-description-2">
    {{{ markdown movie.attributes.intro }}}
  </p>
  
  </div>
  {{#if movie.attributes.image }}
  <img src="{{ movie.attributes.image.url }}" class="movie-poster-2">
  {{/if }}
  <div>
    <a href="/allmovies">Tillbaka till filmerna</a>
  </div>
  
</section>


